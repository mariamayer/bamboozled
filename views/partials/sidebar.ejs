<div class="col-md-3 sidebar">
  <% categories.forEach((category) => { %>
    <div class="cat">
      <a class="title" href="/categories/<%= category.title %>"><%= category.title %></a>
      <p><%= category.description %></p>
    </div>
  <%  }) %>
</div>
